sonar.projectKey=leon36000_ForgeAI-Toolkit
sonar.organization=leon36000

sonar.sources=src,scripts
sonar.tests=tests
# scripts/proof/** exclus de l'ANALYSE : scripts de preuve e2e (déploiement Docker réel + assertions),
# outillage de confiance exécuté manuellement, PAS du code de bibliothèque livré — leurs subprocess /
# urllib http locale sont inhérents à la preuve (pas une surface d'attaque du produit).
sonar.exclusions=**/__pycache__/**,**/*.pyc,src/forgeai/data/**,scripts/proof/**
sonar.test.exclusions=**/__pycache__/**,**/*.pyc

# Risque ACCEPTÉ & DOCUMENTÉ (validé par revue scellée 3/3, S5/S6) — S2612 permissions de fichier :
# le conteneur openbao-unsealer tourne sous l'UID NON-root de l'image officielle (≠ opérateur) et lit
# la clé d'unseal via un bind-mount hôte → le répertoire doit être traversable (0o711) et la clé lisible
# (0o644) par ce conteneur. La clé d'unseal est co-localisée avec le storage scellé sur le MÊME hôte
# (frontière de confiance identique : qui lit l'hôte a déjà les deux) et le ROOT token reste 0o600 ISOLÉ
# hors du volume de l'unsealer. Le contrôle réel = l'accès au nœud (documenté, frontière T3).
sonar.issue.ignore.multicriteria=e1,e2,e3,e4,e5,e6,e7
sonar.issue.ignore.multicriteria.e1.ruleKey=python:S2612
sonar.issue.ignore.multicriteria.e1.resourceKey=src/forgeai/deploy/openbao_flow.py

# Risque ACCEPTÉ & DOCUMENTÉ (déjà réfuté par l'audit v7.1 sous CAND-003, bandit B108, même
# ligne) — S5443 "temporary files in publicly writable directories" sur k3s.py:249 :
# `chemins = ["/tmp"] + ...` n'écrit RIEN sur le disque de ce processus. C'est un littéral de
# CONTENU d'un manifeste Kubernetes généré (chemin de montage `emptyDir` à l'intérieur d'un
# conteneur DISTANT et ÉPHÉMÈRE, cf. `_volumes_inscriptibles`), pas un chemin de fichier
# temporaire créé localement. Le pod généré monte lui-même un `emptyDir` neuf et isolé sur ce
# chemin — aucun autre processus du nœud hôte ne peut y écrire concurremment. Ce constat cause
# le "D Security Rating on New Code" persistant (issue #385) malgré une CAUSE déjà investiguée
# et classée non-issue par la revue scellée de l'audit.
sonar.issue.ignore.multicriteria.e2.ruleKey=python:S5443
sonar.issue.ignore.multicriteria.e2.resourceKey=src/forgeai/renderers/k3s.py

# Risque ACCEPTÉ & DOCUMENTÉ (déjà annoté dans le code — cli.py:73, `# noqa: S105  proof:allow`)
# — S2068 "hard-coded credential" sur la constante d'identifiant immudb dev-chassis : valeur
# PUBLIQUE par défaut de l'image immudb officielle (documentée mondialement par le projet immudb
# lui-même), jamais un identifiant propre à ForgeAI. La rotation de cet identifiant relève du
# durcissement production (hors périmètre E3c) ; l'immuabilité tamper-evident d'immudb n'en
# dépend pas. Le `# noqa: S105` supprime déjà l'avertissement ruff équivalent ; SonarCloud ne le
# comprend pas (mécanisme distinct), d'où cette entrée séparée pour la même justification.
sonar.issue.ignore.multicriteria.e3.ruleKey=python:S2068
sonar.issue.ignore.multicriteria.e3.resourceKey=src/forgeai/cli.py

# Risque ACCEPTÉ & DOCUMENTÉ (déjà justifié dans le code — bootstrap.py:68, commentaire "Headscale
# souverain") — S5332 "HTTP protocole non sécurisé" sur render_join_plan() : la validation
# `login_server.startswith(("http://", "https://"))` autorise DÉLIBÉRÉMENT http:// pour un control
# plane Headscale auto-hébergé sur réseau privé (bootstrap initial, avant toute terminaison TLS).
# Aucune valeur n'est jamais transmise en clair sur un réseau non maîtrisé par ce chemin (le module
# entier documente EX-1/EX-2/EX-3 : aucun secret matérialisé, le plan porte des références).
sonar.issue.ignore.multicriteria.e4.ruleKey=python:S5332
sonar.issue.ignore.multicriteria.e4.resourceKey=src/forgeai/network/bootstrap.py

# Risque ACCEPTÉ & DOCUMENTÉ — FAIT VÉRIFIÉ, pas une hypothèse (issue #385, suite de #416/#417) :
# pythonsecurity:S8705/S8707 ("LLMs running this code with faulty CLI arguments...") continuent
# de se déclencher sur scripts/gate_docs.py et src/forgeai/core/registre.py MALGRÉ la présence
# d'une validation explicite immédiatement avant chaque site (gate_docs.py::_valider_ref_git()
# appelée avant subprocess.run ; gate_docs.py::_charger_json() valide chemin.is_file() avant
# read_text() ; registre.py::_load_key() valide stat.S_ISREG(key_path) avant read_text()) —
# vérifié par lecture directe du code déployé sur `main` après merge (les lignes rapportées par
# le dashboard SonarCloud, décalées par l'ajout de code, correspondent exactement aux lignes déjà
# gardées). Ces règles récentes ("LLM-aware", numérotation S87xx) ne reconnaissent pas de
# validateur personnalisé comme neutralisant le taint — un pattern-match catégoriel, pas une
# analyse de flux qui suivrait la garde. La correction de fond est réelle et testée
# (tests/test_doc032_gate_docs.py, tests/test_registre.py, RED-GREEN prouvé pour 2 des 3 gardes) ;
# seule la reconnaissance automatique par cette règle spécifique échoue.
sonar.issue.ignore.multicriteria.e5.ruleKey=pythonsecurity:S8707
sonar.issue.ignore.multicriteria.e5.resourceKey=scripts/gate_docs.py
sonar.issue.ignore.multicriteria.e6.ruleKey=pythonsecurity:S8705
sonar.issue.ignore.multicriteria.e6.resourceKey=scripts/gate_docs.py
sonar.issue.ignore.multicriteria.e7.ruleKey=pythonsecurity:S8707
sonar.issue.ignore.multicriteria.e7.resourceKey=src/forgeai/core/registre.py

sonar.python.coverage.reportPaths=coverage.xml
sonar.python.version=3.12
sonar.sourceEncoding=UTF-8

# Les assets navigateur (JS/HTML/CSS) sont analysés pour la qualité mais PAS comptés dans la
# couverture : aucun runner JS n'est câblé aux gates (le produit est stdlib pur). Leur logique
# pure est prouvée hors couverture par tests/js/*.test.cjs (Node) et leur rendu se vérifie en
# navigateur. Sans cette exclusion, les lignes JS neuves comptent comme non couvertes et font
# chuter « coverage on new code » sous 80 % (S6b).
# scripts/proof/** : scripts de PREUVE e2e (ex. prove_ubuntu_vierge, prove_openbao_prod) — ce SONT
# les preuves exécutées (déploiement Docker réel), pas du code de bibliothèque unit-testable. Ils
# sont exécutés RÉELLEMENT et leur sortie est journalisée au registre / EVIDENCE ; les compter en
# couverture ferait chuter « coverage on new code » sous 80 % alors qu'ils sont eux-mêmes la preuve.
# scripts/check_metering_sites.py (B-20b) : lanceur CLI MINCE (sys.path + appel de main()). La
# LOGIQUE vit dans forgeai.models.metering_guard, couverte à 100 % par tests/test_metering_guard.py
# (mesurée par --cov=src/forgeai). Le wrapper lui-même n'est que de la plomberie d'entrée.
sonar.coverage.exclusions=src/forgeai/web/assets/**,scripts/proof/**,scripts/check_metering_sites.py
