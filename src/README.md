# src/ — code du ForgeAI Toolkit

Alimenté exclusivement par le pipeline BMAD (§8 du plan maître) : story → TDD →
gates → revue aveugle ×3 → signature Fable → merge. Les mocks sont interdits ici
(autorisés uniquement dans `tests/`, pour simuler des services externes, déclarés
dans le manifeste de la story).
