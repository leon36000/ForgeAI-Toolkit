# B-24 — Confinement filesystem au workspace : décision d'architecture

Décision rendue par l'architecte (Kimi-K3) le 2026-07-29, appliquée telle quelle.
Périmètre de CETTE story (B-24a) : décisions 4 uniquement (validateur partagé).
B-24b (HookSpec), B-24c (génération de la garde), B-24d (comportement + journal)
suivent dans cet ordre. Le critère figé « refusée ET journalisée » est porté par
B-24d seule — aucune story intermédiaire ne le promet.

Adaptation de type journalisée (orchestrateur) : resolve_within accepte
str | os.PathLike et retourne Path — la sémantique de l'architecte (realpath des
deux côtés, commonpath après normcase, exceptions typées) est inchangée ; seuls
les types s'alignent sur le reste du code. Les relecteurs scellés jugent ce choix.

# Décision d'architecture — B-24 : confinement filesystem au workspace

## 1. Signature — type dédié `HookSpec`, union additive

**Décision : introduire une dataclass gelée `HookSpec(event: str, command: str, matcher: str = "*")` et étendre le paramètre en `hooks: list[str | HookSpec]`. Les `str` sont normalisés en interne vers `HookSpec(event=s, command=s, matcher="*")` — exactement le comportement actuel.**

Justification : signature additive à comportement inchangé pour le cas `str`, donc `tests/test_ide_bootstrap.py:68-73` passe sans modification et les appelants existants sont préservés — c'est le même principe que la décision FAI-0006 (paramètre optionnel, défaut qui reproduit l'ancien comportement). Un paramètre séparé `hook_specs: list[HookSpec] | None = None` est rejeté : deux listes parallèles exprimant la même notion est une source permanente de confusion (« lequel gagne ? »). La sérialisation vers le dict JSON reste interne à `generate_governance_config` ; `IdeConfig` ne change pas de forme.

Signature retenue :
`generate_governance_config(skills: list[str], hooks: list[str | HookSpec], *, ide: str = "claude-code") -> IdeConfig`

## 2. Où vit la garde — script autonome généré, interpréteur absolu

**Décision : un script Python stdlib autonome, généré (pas copié depuis un template fichier : une constante chaîne dans le produit) à `<dest>/.claude/hooks/forgeai_guard_fs.py`. La commande écrite dans settings.json est `"<sys.executable_capturé_au_bootstrap>" "<chemin_absolu_du_script>"`.**

Justification triple. (a) *PATH* : la commande ne dépend ni de `forgeai` dans le PATH ni même de `python` dans le PATH — `sys.executable` au moment du bootstrap est un chemin valide sur la machine de l'utilisateur par construction. (b) *Survie* : la garde est une protection ; elle ne doit pas mourir avec la désinstallation ou le changement de venv de l'outil qui l'a posée — un `forgeai guard-fs` (module du produit) transformerait une mise à jour de forgeai en fenêtre de trou ou en panne, au gré du fail-open/fail-closed de l'IDE. (c) *Zéro dépendance préservée* : le script n'importe que stdlib et n'importe pas forgeai, donc aucune couplage de version. Coût assumé : ~30 lignes d'append JSONL dupliquées dans le template (voir §5), duplicat documenté comme contrat de format, pas comme logique partagée. Le script lit la charge JSON du hook sur stdin, répond selon le contrat de sortie de l'IDE cible (code de sortie de refus + message sur stderr), et **toute exception interne du script produit un refus** (fail-closed) : une garde de sécurité qui crash en autorisant est un trou, pas une garde.

## 3. « Hors workspace » — racine figée au bootstrap, realpath des deux côtés, exceptions hors de portée de l'agent

**Racine.** C'est `dest_dir` passé à `write_ide_config`, canonisé par `os.path.realpath` **au bootstrap** et embarqué en constante dans le script généré. Justification : figer la racine dans le script évite toute dérive par `cwd` à l'exécution ; `realpath` (pas `abspath`) au bootstrap car sur macOS `/tmp` → `/private/tmp` et un home symlinké produirait sinon des faux positifs de masse.

**Cible.** Chaque chemin extrait de la charge est résolu par `os.path.realpath` contre le `cwd` fourni par la charge ; `..` et les relatifs disparaissent dans la résolution réelle ; la comparaison utilise `os.path.commonpath` après `os.path.normcase` (Windows : `C:\Repo` vs `c:\repo` sinon = faux positif immédiat). Résoudre les symlinks **des deux côtés** est non négociable : ne résoudre que la cible laisse passer un symlink sortant (faux négatif) ; ne résoudre que la racine casse tout symlink entrant légitime (faux positif).

**Exceptions.** Oui, obligatoires — sinon `pip`, tout compilateur et tout outil écrivant dans le tempdir rendent le produit inutilisable, et un confinement contourné par l'utilisateur agacé est pire que pas de confinement. Décision : la liste vit dans `~/.forgeai/guard-fs.allow` (une ligne = un chemin réel préfixe), **jamais dans le workspace** — un fichier d'exceptions dans le workspace est modifiable par l'agent confiné, ce qui revient à laisser le prisonnier tenir la liste de ses permissions. Défaut livré : le tempdir système résolu (`tempfile.gettempdir()` passé par `realpath`), en lecture-écriture. Aucune autre exception implicite : tout le reste (cache pip, etc.) est un choix explicite de l'utilisateur, éclairé par le fait que chaque refus journalise le chemin exact refusé. Le script, processus non confiné de l'utilisateur, peut lire ce fichier ; l'agent ne peut pas l'écrire sans déclencher le refus qu'il cherche à lever.

## 4. Validateur partagé — extraction dans `core/validation.py`, refactor de `local.py` inclus

**Décision : extraire `resolve_within(path: str, root: str, *, base: str | None = None) -> str` (lève `ValidationError`) dans `core/validation.py`, et refactorer `models/local.py:85-91` pour l'utiliser dans la MÊME story.**

Justification : un validateur « partagé » livré avec un seul consommateur pendant que l'ancien duplicat survit à 20 lignes de là, c'est exactement la duplication que l'extraction prétendait éliminer — l'extraction sans refactor est une promesse non tenue déguisée. Le risque du refactor est borné par les tests existants de `local.py` ; le validateur doit exposer aussi la primitive « nom simple » (rejet de `/`, `os.sep`, `..`) car `local.py` valide un *nom de fichier* avant de valider un *chemin*, et confondre les deux ferait régresser son comportement. `core/validation.py` plutôt qu'un nouveau module : il existe, c'est son rôle déclaré, et un module par fonction est du sur-découpage.

## 5. Journalisation — registre hors workspace, refus inconditionnel

**Décision : chemin `~/.forgeai/Registres/mission.jsonl` (surchargeable via la variable d'environnement `FORGEAI_REGISTRE` lue par le script), `type_ = "guard_fs_denied"`, `actor = "guard-fs"`, payload : chemin demandé, chemin résolu, racine, outil, cwd, horodatage.**

Justification du chemin : le registre hors workspace est **hors de portée d'écriture de l'agent confiné** — il ne peut ni l'effacer ni le falsifier, ce qui est précisément la propriété qu'on exige d'un journal d'audit de sécurité. Le script, non confiné, peut y écrire. Le script n'important pas forgeai, il embarque un append JSONL minimal conforme au schéma de ligne de `core/registre.py` (verrouillage `fcntl` POSIX / `msvcrt` Windows, fsync) — duplication contrôlée d'un *format*, assumée et couverte par un test de compatibilité : une ligne écrite par le script doit être lisible par le lecteur officiel du registre.

**Si l'écriture échoue : le refus tient, toujours.** Justification : l'inverse (autoriser quand le log est KO) fait de la journalisation un kill-switch de la sécurité — il suffirait à l'agent de saturer le disque ou de rendre le registre inaccessible pour sortir librement. L'échec de log est rapporté dans le message de refus (stderr, visible par l'utilisateur) ; il n'y a pas de fallback d'écriture dans le workspace, qu'un agent confiné pourrait effacer.

## 6. Découpage — quatre stories, le critère figé porté par la dernière

Une seule story mélangerait refactor, modèle de données, génération et comportement runtime : improuvable proprement. Découpage ordonné :

1. **B-24a — Validateur partagé.** Critère : `resolve_within` rejette `..`, symlink sortant, chemin hors racine après `normcase` ; accepte symlink entrant et racine symlinkée (cas macOS `/tmp`) ; `models/local.py` refactoré, ses tests existants verts.
2. **B-24b — `HookSpec`.** Critère : `test_ide_bootstrap.py:68-73` vert sans modification ; nouveau test assertant la forme exacte `{event: [{matcher, hooks: [{type: "command", command}]}]}` avec événement ≠ commande et matcher ≠ `"*"`.
3. **B-24c — Génération de la garde.** Critère : le bootstrap écrit le script (existant, non vide) et le hook `PreToolUse` dont la commande est un interpréteur absolu + chemin absolu du script ; la racine canonique est embarquée dans le script.
4. **B-24d — Comportement + journal (critère figé B-24).** Critère : e2e, exécution du script généré — charge pointant hors racine (résolue) → refus **et** ligne `guard_fs_denied` lisible par le lecteur officiel du registre ; charge dans la racine → autorisation ; registre inaccessible → refus maintenu.

Le critère figé « refusée ET journalisée » est inséparable : il est donc le critère unique de B-24d, et aucune story intermédiaire ne le promet.

## 7. Le piège que tu n'as pas vu — deux, dont un qui menace le critère figé

**Piège majeur — le matcher `PreToolUse` ne voit pas Bash.** Le hook reçoit des charges structurées pour les outils fichier (`Read`, `Write`, `Edit` : `file_path` explicite), mais pour `Bash` il reçoit une *chaîne de commande*. Confiner `cat /etc/passwd`, `cd /etc && cat passwd`, `base64 -d <<< … | sh` ou `python -c "open('/etc/passwd')"` par analyse textuelle est indécidable : toute tokenisation `shlex` se fait déborder par les substitutions, l'expansion de variables et l'encodage. Un hook qui prétend tenir « **tout** accès en lecture/écriture » (critère figé) avec Bash dans le matcher vend une promesse algébriquement fausse. Décision : le matcher couvre les outils fichier + `Bash` avec une politique « chemins littéraux résolubles uniquement » — chaque token de forme chemin est vérifié via `resolve_within`, et la documentation produit écrit noir sur blanc que l'exécution de code arbitraire via Bash n'est pas confinable sans sandbox OS (hors périmètre zéro-dépendance). **Conséquence non négociable : le critère d'acceptation de B-24d est reformulé avant codage en « tout accès fichier hors workspace via les outils structurés, et tout chemin littéral hors workspace dans une commande Bash, est refusé et journalisé »** — sinon on livre un critère non tenu, ce qui est pire qu'un critère honnête et réduit.

**Piège second — la garde est auto-révocable par le confiné.** `.claude/settings.json` et le script de garde vivent *dans* le workspace : l'agent a le droit d'y écrire, donc peut retirer son propre hook ou réécrire la garde. Décision corrective intégrée à B-24d : la racine de confinement exclut **en écriture** `.claude/settings.json`, `.claude/hooks/` et le script lui-même — la garde se protège elle-même, sinon elle est du théâtre.

Et deux risques connus à documenter sans prétendre les résoudre : la course symlink TOCTOU entre validation et accès (irréductible au niveau hook, sandbox OS requise), et la divergence de verrouillage POSIX/Windows du registre, couverte par le test de compatibilité de format de B-24d.

---

# ADR B-24 (addendum) — extraction des chemins Bash : décision du tour 6

Contexte : le repli de tokenisation a été corrigé 3 fois et rejeté 3 fois par la
revue scellée. Signal de conception, pas bug isolé. Escaladé à l'architecte avec
mesures. Décision rendue le 2026-07-29 (MiniMax-M3, Kimi-K3 ayant rendu du code
au lieu d'une décision), appliquée telle quelle. L'option fail-closed a été
ÉCARTÉE PAR LES DONNÉES : shlex lève sur "echo l'utilisateur" et "grep don't f",
commandes triviales — la refuser rendrait le produit inutilisable.

1. **Option retenue : C — extraction par expression régulière, suppression de la tokenisation.** Le repli par `re.split` n'est pas un fix, c'est l'aveu qu'on réimplémente un tokeniseur shell par soustraction de caractères, et trois tours de revue scellée ont déjà trouvé trois oublis successifs : c'est un signal de conception, pas une coïserie. L'option C inverse la perspective — au lieu d'essayer de *séparer* correctement des tokens pour ensuite *chercher* des chemins dedans, on *extrait* directement les suites « qui ressemblent à un chemin » où qu'elles soient dans la chaîne brute. Une suite contenant un séparateur et composée de caractères hors des délimiteurs shell ne peut pas se cacher : si `/etc/passwd` est dans la commande, le motif le trouve, qu'il soit entre guillemets, après une redirection, dans une substitution de commande ou dans un heredoc. La dichotomie nominal/repli disparaît, donc la porte de service disparaît avec elle. L'objection « on perd la sémantique des mots » est exacte mais hors périmètre : on n'a jamais prétendu interpréter la commande, on a prétendu en extraire les chemins littéraux — ce que le motif fait, exhaustivement et sans lever.

2. **Backslash d'échappement : retrait avant résolution, sur POSIX uniquement.** Un token `\/etc/passwd` est, après retrait du backslash, `/etc/passwd` — chemin absolu hors racine, donc refusé. Sans retrait, il se résout contre le cwd en `<\/etc/passwd`, qui est *dans* la racine : autorisé à tort, contournement trivial. La mesure confirme la vulnérabilité, donc la décision s'impose. Le retrait se fait *après* l'extraction par regex et *avant* la résolution `Path`, par une simple substitution `token.replace(chr(92), "")`. Sur Windows le backslash est un séparateur natif, pas un échappement : on l'y conserve. La logique est donc plate et localisable — `if os.sep != chr(92): token = token.replace(chr(92), "")` — et elle se teste par deux cas symétriques, un POSIX qui exige le retrait, un Windows qui ne le fait pas.

3. **Coût assumé.** Premier coût : les chemins contenant un caractère shell légitime — espace dans un nom de fichier, par exemple `mon dossier/f.txt` — ne sont pas captés en entier par le motif, qui s'arrête au délimiteur. C'est acceptable parce que (a) le périmètre est un workspace de projet, où les espaces dans les noms de fichiers sont rares et évitables, et (b) un script malveillant n'utilise pas de tels noms pour cibler une garde. Deuxième coût : un chemin dont tous les caractères sont des délimiteurs shell — pure absurdité syntaxique — n'est pas extrait. Acceptable aussi : un tel « chemin » n'existe pas. Troisième coût, déjà connu et explicitement reporté dans le périmètre : `$(cmd)`, backticks et `$VAR` ne sont pas *interprétés*. Si un attaquant écrit `cat $(echo /etc/passwd)`, le motif extrait `/etc/passwd` et la garde remplit son office ; s'il écrit `cat $CIBLE` où `CIBLE=/etc/passwd`, on ne voit qu'un mot sans séparateur, qui passe. C'est documenté comme hors périmètre dès l'origine. Quatrième coût : on perd la distinction syntaxique « ceci est un chemin et cela est une option » au profit d'une distinction purement lexicale « ceci contient un séparateur ». La résolution + test d'appartenance à la racinerat rattrape ce qu'il faut, et le filtre « préfixe `.` » conservé en complément rattrape la cible nue `rm -rf .claude` qui, elle, ne contient pas de séparateur.

4. **Documentation produit.** Il faut écrire, au chapitre « Limites connues » de la garde filesystem, trois phrases exactement : « La garde extrait les chemins *littéraux* présents dans la commande. Elle n'interprète pas les substitutions (`$VAR`, `$(cmd)`, backticks) et n'évalue pas les globs (`*`, `?`, `[…]`) : un attaquant qui s'appuie sur l'expansion du shell pour matérialiser un chemin hors racine n'est pas arrêté par cette garde — c'est le rôle d'une sandbox OS, explicitement hors périmètre. Les chemins contenant des espaces ne sont pas reconnus en tant que tels : nommez vos fichiers sans espace, c'est la convention du projet. Les redirections, pipes, here-docs et chaînes entre guillemets sont analysés : un chemin entouré de `"…"` ou apparaissant après un `>` ou dans un heredoc est extrait et contrôlé. » Cette formulation assume le périmètre, ne le cache pas, et place la responsabilité de l'expansion hors du produit.

5. **Critère de test.** Une suite de tests dans `tests/test_guard_fs.py`, organisés en trois axes. **Axe A — vecteurs d'évasion captés** : les sept cas de la mesure (entre guillemets doubles, entre guillemets simples, après redirection, dans une substitution de commande, dans un heredoc avec apostrophe française non fermée, en chemin absolu POSIX, en chemin Windows avec backslash) produisent chacun la liste de chemins attendue. **Axe B — non-régression syntaxique** : `echo l'utilisateur`, `grep don't fichier.txt`, `ls *.py`, `cat $HOME/truc` — les deux premiers ne lèvent pas et ne produisent aucun chemin (pas de faux positif), le troisième non plus (glob non interprété), le quatrième non plus (variable non interprétée, et le mot nu `HOME/truc` n'a pas de séparateur en tête). **Axe C — symétrie backslash** : un même token `\/etc/passwd` est, sur POSIX, résolu et refusé, et sur Windows (où `os.sep == chr(92)`) laissé tel quel et résolu ; les deux assertions sont dans le même fichier, paramétrées par `sys.platform` ou un monkeypatch de `os.sep`, et les deux passent. Un quatrième axe, **D — appartenance à la racine**, vérifie que les chemins *extraits* sont ensuite correctement filtrés : un chemin sous la racine passe, un chemin hors racine est refusé avec un verdict qui cite le chemin résolu, pas le token brut — c'est ce qui prouve que la résolution a bien eu lieu après extraction. Si ces quatre axes passent, la décision est tenue.

## Écart mesuré vs le critère de test de l'architecte (point 5, axe B)
L'architecte annonce que `cat $HOME/truc` « ne produit aucun chemin ». MESURE :
le motif exclut `$`, donc le fragment `HOME/truc` EST capté comme candidat ;
il est simplement AUTORISÉ (résolu sous la racine). Le test doit donc asserter
exit=0, PAS l'absence de candidat. Corrigé dans la spec d'implémentation.

## Simplification retenue à l'implémentation (mesurée, 15/15 conforme)
Plutôt que deux mécanismes (regex de motifs + filtre des tokens nus), UN SEUL
découpage sur la classe des délimiteurs shell `[\s<>;|&()\"'\`$]+`, suivi du
filtre `_ressemble_chemin` existant. Équivalent au motif de l'option C et couvre
aussi la cible nue `rm -rf .claude` (et `rm -rf>.claude`, collé), sans shlex
donc sans exception possible : le chemin dégradé disparaît.

---

# ADR B-24 (amendement du tour 11) — union des extractions

Decision rendue le 2026-07-29 apres escalade, appliquee telle quelle.
Declencheur : le tour 11 a revele une CLASSE entiere de contournements (scission
d'un nom par des guillemets : bash CONCATENE les parties quotees adjacentes, notre
decoupage les SEPARE). Mesure : `echo bad > .cl"aude/settings.json"` autorisait
le confine a ecraser la configuration de sa propre garde.

1. **Option retenue : l'UNION des deux extractions, en permanence et sans hiérarchie de repli.** La réduction du périmètre aux outils structurés est rejetée : elle transforme la garde enrésidu décoratif sur le vecteur d'attaque principal (Bash), et un agent qui découvre qu'on l'a délesté de la moitié de sa surface de défense n'a aucune raison de ne pas y aller. La troisième voie —parser Bash sérieusement, avec un vrai lexer— est hors périmètre produit : maintenir un parser shell correct face à toutes les subtilités POSIX/Windows est un métier, et le pack de revue l'a déjà implicitement condamné en démontrant que chaque itération trouve un nouvel oubli. L'union est la seule décision qui ne sacrifie ni la sémantique de concaténation, ni la garantie « ne lève jamais », et qui accepte le coût mesuré au lieu de le nier.

2. **Pourquoi la réintroduction de shlex n'est pas un retour en arrière.** L'ADR du tour 6 avait supprimé shlex parce qu'il était utilisé comme **étape primaire avec repli**, et que le repli était une porte : un attaquant forçant le `ValueError` obtenait un mode dégradé plus permissif que le mode nominal. La décision actuelle inverse exactement le statut : shlex n'est plus le primaire dont on espère le succès, c'est un **enrichisseur** dont on accepte l'échec sans conséquence. La propriété garantissant l'absence de nouvelle porte est strictement celle-ci : « le découpage strict est appliqué dans **tous les cas**, y compris quand shlex réussit ». Si shlex lève, on perd un jeu de candidats, on n'en change pas la rigueur. Si shlex réussit, on ajoute des candidats qui sont des concaténations plus fidèles à ce que bash exécutera réellement. Aucun chemin ne mène à un mode plus permissif que le découpage strict seul. La garantie est monotone : l'union ne peut qu'élargir l'ensemble contrôlé, jamais le réduire.

3. **Coût assumé.** L'union extrait des fragments qui ne sont pas, à strictement parler, des arguments distincts de bash — par exemple `dd if=/etc/passwd` produit `if=/etc/passwd` en plus du chemin réel. Ces artefacts sont filtrés en aval par le test « ressemble à un chemin » puis par la résolution : `if=/etc/passwd` ne commence ni par `/`, ni par `~`, ni par `.`, ni par une lettre Windows, donc il est éliminé avant même la résolution. Le coût véritable est donc ailleurs : il est **psychologique et doctrinal**. Nous acceptons officiellement que l'extraction produit des sur-ensembles, et que la décision de blocage n'est jamais « cette chaîne apparaît textuellement dans la commande » mais « un chemin littéral résolvable hors racine, extrait par au moins une des deux méthodes, pointe hors workspace ». Cette nuance doit être écrite, sinon le prochain relecteur croira avoir trouvé un faux positif et rouvrira le cycle. Second coût assumé : shlex est stdlib pure et POSIX-first ; sur Windows, ses messages d'erreur peuvent être sensiblement différents. On ne s'en sert que comme extracteur, pas comme interpréteur, donc la sortie tokenisée reste utilisée comme matière brute filtrée — Windows n'est pas discriminé.

4. **Documentation produit.** Trois lignes, pas trois paragraphes. Premièrement, dans la description de la garde : « Les commandes Bash sont contrôlées par extraction des fragments ressemblant à un chemin littéral, selon deux méthodes indépendantes dont l'union est filtrée. Une commande qui concatène des fragments entre guillemets (ex. `.cl"aude/settings.json"`) est traitée comme bash la traite. Une commande syntaxiquement invalide pour un parser shell (guillemet orphelin) reste contrôlée par la méthode indépendante. » Deuxièmement, dans la section limites : « Cette garde n'est pas un parser Bash. Elle ne prétend ni interpréter les redirections, ni résoudre les substitutions de commandes, ni évaluer les expansions de variables. Un chemin qui n'apparaît jamais textuellement dans la commande (par exemple via une variable d'environnement interpolée) n'est pas vu par cette garde. Le confinement d'un agent passant par Bash exige, en complément, une sandbox OS — cette garde est un filtre de surface, pas une enclave. » Troisièmement, dans le changelog : « B-24 (amendement) : union des extractions shlex et découpage strict, suppression de la notion de repli. »

5. **Critère de test et critère d'arrêt.** Le critère de tenue est une **matrice de cas reproductible**, exécutée comme test unitaire sur le script généré, comportant au minimum : les six cas mesurés du pack (concaténation entre guillemets sur cible `.claude`, lecteur Windows quoté, concaténation `/etc`/passwd, guillemet orphelin, redirection collée sans espace, argument `dd if=…`) ; les trois non-régressions du pack (`echo l'utilisateur`, `echo bonjour`, `ls *.py`) ; et au moins un cas de **rejet intentionnel** où l'union extrait un fragment qui n'est pas un chemin (`-Iinclude/sys`) pour prouver que le filtre aval n'a pas été affaibli. Le critère d'arrêt est plus strict que d'habitude et il faut l'écrire noir sur blanc : on considère la story close **non pas** quand plus aucune objection n'est formulée, mais quand **deux conditions simultanées** sont remplies. D'abord, la matrice passe en entier sans qu'aucun cas attendu-autorisé ne soit refusé, ni aucun cas attendu-refusé ne soit autorisé, et ce sur les trois OS supportés — Linux, macOS, Windows, avec un runner par OS, pas seulement Linux. Ensuite, **trois relecteurs distincts**, choisis hors de la boucle qui a déjà tourné, relisent le diff complet de l'amendement et n'annoncent **aucune nouvelle classe** de contournement. Si l'un des trois en annonce une, on itère ; si aucun n'en annonce, on clôt. Onze tours ont déjà eu lieu, et il est probable que d'autres en trouveront encore — ce n'est pas un motif pour ne pas fixer un seuil d'arrêt. Le seuil existe précisément pour que la prochaine objection soit traitée comme la preuve qu'il faut itérer, non comme la preuve qu'on n'en finit jamais.

## Ecarts de capacite constates a l'application (a ne pas masquer)
1. CRITERE D'ARRET, volet « 3 OS » : la CI du depot est aujourd'hui
   100 % ubuntu-latest, sans aucune strategie de matrice (verifie :
   `grep runs-on .github/workflows/*.yml` -> 9 occurrences, toutes ubuntu).
   Le critere n'est donc PAS satisfiable en l'etat. Une matrice multi-OS ciblee
   sur tests/test_guard_fs.py est ajoutee dans cette story : c'est le seul
   composant dont le comportement depend de l'OS (separateurs, lecteurs).
2. CRITERE D'ARRET, volet « 3 relecteurs hors boucle » : DeepSeek, Gemini et Grok
   ont tous relu cette story. Un trio hors boucle est possible avec des vendors
   distincts n'ayant ni code ni relu (MiMo-Pro-V2 / Qwen / Nemotron-3-Ultra),
   sous reserve de stabilite de leurs routes — a verifier par mesure, pas par
   hypothese.

### Resolution des ecarts (2026-07-29, ajout — les constats ci-dessus restent tels quels)
1. RESOLU. La matrice `guard-fs-multi-os` (ubuntu/macos/windows, fail-fast: false)
   est en place et VERTE sur les trois OS au commit 4611e98 : windows-latest a
   execute 126 tests (124 passed, 2 skipped, 0 echec — log CI run 30504340202).
   Son premier passage (10 echecs sur windows) a ete depouille cas par cas :
   8 attentes de test non portables corrigees (commit 4611e98), et 1 defaut
   PRODUIT revele hors perimetre de la garde — `core/registre.py` et
   `models/_locking.py` importent `fcntl` (POSIX-only) sans condition, ce qui
   rend le registre hash-chaine inutilisable sur Windows : trace en issue #286,
   deliberement NON corrige ici (le verrou protege contre la corruption
   concurrente ; le degrader en no-op silencieux serait pire que l'echec franc).
   Dans les 9 cas ou la garde elle-meme a ete sollicitee par le runner Windows,
   elle a emis le bon verdict avec le bon motif : aucune ligne de
   `src/forgeai/ide/guard_fs.py` n'a du changer.
2. RESOLU. Le trio hors boucle MiMo-Pro-V2 / Qwen3.7-Max / Nemotron-3-Ultra
   (routes verifiees par mesure avant usage) a scelle la revue de cloture
   APPROVE 3/3 (sceau 3a7a8af67859). Le correctif de portabilite (4611e98,
   posterieur a cette cloture) a recu sa propre revue scellee APPROVE 3/3 par
   DeepSeek-V4-Pro / Gemini-3.1-Pro-OR / Grok-4.5 (sceau f5783798a718,
   verdicts sous reviews/B-24e/), SWAP CIV respecte — les codeurs des lots
   etaient MiniMax-M3 et Kimi-K3, vendors absents du trio.
