# Sandbox/ — expérimentations jetables

Zone hors gates pour prototypes et essais. Rien ici n'est une preuve, rien ici
n'est mergeable vers `src/` sans repasser par le pipeline complet (story BMAD,
TDD, gates, revue aveugle ×3).
