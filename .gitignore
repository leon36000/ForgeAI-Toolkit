# Claude Code session-local (cockpit Forge — pas le produit)
.claude/worktrees/
.claude/settings.local.json
.mcp.json
CLAUDE.local.md

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.coverage
.mypy_cache/
.ruff_cache/

# OS / editors
.DS_Store
*.swp

# Artefacts d exécution locale
run/

# Artefacts de build
dist/
build/
*.egg-info/
# stories/ : specs de story = artefacts de gouvernance versionnés (référencés au registre) ;
# le reste du dossier est éphémère. NB : ignorer le CONTENU (stories/*), pas le dossier, pour
# que l'exception !stories/*.md fonctionne (git ne réinclut pas sous un dossier exclu).
stories/*
!stories/*.md
